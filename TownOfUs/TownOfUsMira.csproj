<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <LangVersion>preview</LangVersion>
        <Description>Town Of Us Mira</Description>
        <RootNamespace>TownOfUs</RootNamespace>
        <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
        <Version>$(VersionPrefix)-$(VersionSuffix)</Version>
    </PropertyGroup>

    <ItemGroup>
        <EmbeddedResource Include="Resources\**\*.*" />
        <None Include="../LICENSE.txt" Pack="true" PackagePath="\"/>
        <None Include="../README.md" Pack="true" PackagePath="\"/>
    </ItemGroup>

    <Import Project="../AmongUs.props" />
    <Target Name="CopyDllToAnotherFolder" AfterTargets="Build">
  <Copy SourceFiles="$(TargetDir)$(TargetName).dll" 
        DestinationFolder="/Users/neiljain/Library/Application Support/CrossOver/Bottles/Main/drive_c/Program Files (x86)/Steam/steamapps/common/Among Us/BepInEx/plugins" 
        SkipUnchangedFiles="true" />
</Target>

</Project>